# Configuration for the documentation site published to GitHub Pages.
# Seeded from `zensical new`; reference at https://zensical.org/docs/setup/basics/

[project]
site_name = "actdocs-rs"
site_description = "Documentation for GitHub Actions and reusable workflows, generated from the manifests themselves."
site_url = "https://fredrikbakken.github.io/actdocs-rs/"

repo_url = "https://github.com/FredrikBakken/actdocs-rs"
repo_name = "FredrikBakken/actdocs-rs"
edit_uri = "edit/main/docs/"

copyright = "Copyright &copy; 2026 Fredrik Bakken"

# Both are the defaults, stated anyway because two other files depend on them:
# `/site` in .gitignore, and `path: site` in the publishing workflow.
docs_dir = "docs"
site_dir = "site"

# Declared rather than inferred from the directory tree, so that adding a page
# is a deliberate act and the order is not alphabetical by accident.
nav = [
  { "Home" = "index.md" },
  { "Usage" = [
    "usage/cli.md",
    "usage/configuration.md",
    "usage/markers.md",
    "usage/pinning.md",
    "usage/hooks.md",
  ] },
  { "About" = [
    "about/development.md",
    "about/credits.md",
    "about/license.md",
  ] },
]

[project.theme]
language = "en"
features = [
  "content.action.edit",
  "content.action.view",
  "content.code.annotate",
  "content.code.copy",
  "content.code.select",
  "content.footnote.tooltips",
  "content.tabs.link",
  "content.tooltips",
  "navigation.footer",
  "navigation.indexes",
  "navigation.instant",
  "navigation.instant.prefetch",
  "navigation.path",
  "navigation.sections",
  "navigation.top",
  "navigation.tracking",
  "search.highlight",
  "toc.follow",
]

# Follows the visitor's operating system by default, and the toggle cycles
# system -> light -> dark -> system.
[[project.theme.palette]]
media = "(prefers-color-scheme)"
toggle.icon = "lucide/sun-moon"
toggle.name = "Switch to light mode"

[[project.theme.palette]]
media = "(prefers-color-scheme: light)"
scheme = "default"
toggle.icon = "lucide/sun"
toggle.name = "Switch to dark mode"

[[project.theme.palette]]
media = "(prefers-color-scheme: dark)"
scheme = "slate"
toggle.icon = "lucide/moon"
toggle.name = "Switch to system preference"

[[project.extra.social]]
icon = "fontawesome/brands/github"
link = "https://github.com/FredrikBakken/actdocs-rs"

[project.markdown_extensions]
abbr = {}
admonition = {}
attr_list = {}
def_list = {}
footnotes = {}
md_in_html = {}
toc.permalink = true
pymdownx.arithmatex.generic = true
pymdownx.betterem = {}
pymdownx.caret = {}
pymdownx.details = {}
pymdownx.emoji.emoji_generator = "zensical.extensions.emoji.to_svg"
pymdownx.emoji.emoji_index = "zensical.extensions.emoji.twemoji"
pymdownx.highlight.anchor_linenums = true
pymdownx.highlight.line_spans = "__span"
pymdownx.highlight.pygments_lang_class = true
pymdownx.inlinehilite = {}
pymdownx.keys = {}
pymdownx.magiclink = {}
pymdownx.mark = {}
pymdownx.smartsymbols = {}
pymdownx.superfences.custom_fences = [
  { name = "mermaid", class = "mermaid", format = "pymdownx.superfences.fence_code_format" },
]
pymdownx.tabbed.alternate_style = true
pymdownx.tabbed.combine_header_slug = true
pymdownx.tasklist.custom_checkbox = true
pymdownx.tilde = {}
